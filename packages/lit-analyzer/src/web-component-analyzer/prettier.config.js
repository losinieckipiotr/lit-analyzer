/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
	arrowParens: "avoid",
	trailingComma: "none",
	endOfLine: "auto"
};

export default config;
